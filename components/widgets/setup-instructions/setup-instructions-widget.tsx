import type { WidgetProps } from "@/lib/widgets/types";

export default function SetupInstructionsWidget({ width: _width, height: _height }: WidgetProps) {
  return <p>Setup instructions Widget</p>;
}
